define([
], function(
) {
'use strict';
return ['$rootScope', function($rootScope) {
    return {
        pathTypeOptions: ['全部', '普通缓存路径', '系统缓存路径', '广告路径', '正则缓存路径', '应用主目录']
    };
    // 结束 
}];
});
